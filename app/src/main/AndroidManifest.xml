<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agentra"
        tools:targetApi="35">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Agentra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Agentra"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.DebugActivity"
            android:exported="false"
            android:theme="@style/Theme.Agentra"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.TestToolsActivity"
            android:exported="false"
            android:theme="@style/Theme.Agentra"
            android:parentActivityName=".ui.SettingsActivity" />

        <service
            android:name=".service.AgentAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityService"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Wake Word Detection Service -->
        <service
            android:name=".service.WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone"
            tools:ignore="ExportedService">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="wake_word_detection" />
        </service>

        <!-- Default Assistant Service (VoiceInteractionService) -->
        <service
            android:name=".service.AssistantService"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
        </service>

        <!-- Assistant Session Service (required companion to VoiceInteractionService) -->
        <service
            android:name=".service.AssistantSessionService"
            android:exported="false"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />

        <!-- Assistant Recognition Service (required for VoiceInteractionService validity) -->
        <service
            android:name=".service.AssistantRecognitionService"
            android:exported="false"
            android:permission="android.permission.BIND_RECOGNITION_SERVICE" />

        <!-- Floating Button Overlay Service -->
        <service
            android:name=".service.FloatingPromptService"
            android:exported="false"
            android:foregroundServiceType="systemExempted"
            tools:ignore="ExportedService" />

        <!-- Notification Reader Service -->
        <service
            android:name=".hierarchy.NotificationReader"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
